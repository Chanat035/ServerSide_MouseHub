<!DOCTYPE html>
<html lang="th">

<head>
  <meta charset="UTF-8">
  <title>MouseHub</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- ✅ Bootstrap CSS -->
  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <!-- Bootstrap Icons -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">
  <!-- Google Font -->
  <link href="https://fonts.googleapis.com/css2?family=Prompt:wght@400;500;600&display=swap" rel="stylesheet">

  <link rel="stylesheet" href="partials/style.css">
</head>

<body>
  <%- include('partials/header') %>
    <%- include('partials/login') %>
      <%- include('partials/register') %>

        <div class="container mt-5 text-center">

          <h1 style="color:#ffb500; margin-bottom:30px;">Welcome to MouseHub</h1>
          <p style="color:#fff; margin-bottom:50px;">เลือกสินค้าที่คุณชื่นชอบและสนุกกับการช้อปปิ้ง!</p>

          <!-- Carousel -->
          <div id="mainCarousel" class="carousel slide mx-auto" data-bs-ride="carousel"
            style="width:900px; height:500px;">
            <div class="carousel-inner h-100">
              <div class="carousel-item active h-100">
                <img src="/images/mouse1.jpg" class="d-block w-100 h-100 rounded" alt="Mouse 1">
              </div>
              <div class="carousel-item h-100">
                <img src="/images/mouse2.jpg" class="d-block w-100 h-100 rounded" alt="Mouse 2">
              </div>
              <div class="carousel-item h-100">
                <img src="/images/mouse3.jpg" class="d-block w-100 h-100 rounded" alt="Accessories">
              </div>
            </div>

            <!-- Controls -->
            <button class="carousel-control-prev" type="button" data-bs-target="#mainCarousel" data-bs-slide="prev">
              <span class="carousel-control-prev-icon" aria-hidden="true"></span>
              <span class="visually-hidden">Previous</span>
            </button>
            <button class="carousel-control-next" type="button" data-bs-target="#mainCarousel" data-bs-slide="next">
              <span class="carousel-control-next-icon" aria-hidden="true"></span>
              <span class="visually-hidden">Next</span>
            </button>
          </div>

          <% if (typeof loginError !=="undefined" ) { %>
            <div class="alert alert-danger mt-4">
              <%= loginError %>
            </div>
            <script>
              document.addEventListener("DOMContentLoaded", function () {
                const loginModal = new bootstrap.Modal(document.getElementById("loginModal"));
                loginModal.show();
              });
            </script>
            <% } %>

        </div>

        <script>
          // ตั้งเวลาเลื่อนอัตโนมัติ 3 วินาที
          const carousel = document.querySelector('#mainCarousel');
          const bsCarousel = new bootstrap.Carousel(carousel, {
            interval: 3000,
            ride: 'carousel'
          });
        </script>
        <!-- ✅ Bootstrap JS (จำเป็นมาก) -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>


</body>

</html>